#include<stdio.h>
int main() {
     
    int a , b ;

    printf("put the value of a : ");
    scanf("%d",&a);


    printf("put the value of b : ");
    scanf("%d",&b);


    a>b ? printf("a is greater than b") : printf("b is greater than a ");

// ? this stands for if  and : this stands for else


  
 return 0;
}